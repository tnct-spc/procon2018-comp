#include "evaluateparam.h"

const std::pair<double,bool> EvaluateParam::evaluateMove(int move){

    std::vector<int> x_list = {1, 1, 1, 0,  0, -1, -1, -1, 0};
    std::vector<int> y_list = {-1, 0, 1, -1, 1, -1, 0, 1, 0};

}
